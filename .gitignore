# ==========================================
# Шаблон .gitignore для проектов на Python
# Файлы и папки, которые следует игнорировать
# ==========================================

# Среда выполнения Python (Python environment)
# --------------------------------------------

# Папка для кэшированных файлов байт-кода
__pycache__/
# Файлы скомпилированного байт-кода
*.pyc
# Временные файлы, создаваемые при оптимизации
*.pyo
# Файлы локализации (если используются)
*.mo
*.pot

# Виртуальные окружения (Virtual environments)
# ---------------------------------------------
# Игнорирование стандартных названий папок для виртуальных окружений
.venv/
venv/
env/
ENV/
# Если вы используете pipenv
.pipenv/
# Если вы используете poetry (обычно размещается вне проекта, но на всякий случай)
.poetry/

# Зависимости и пакеты (Dependencies and packages)
# ------------------------------------------------
# Пакеты, установленные в develop-режиме (egg-info)
*.egg-info/
.eggs/

# Операционная система и IDE (OS and IDE related files)
# -----------------------------------------------------
# Файлы macOS
.DS_Store
# Файлы эскизов Windows
Thumbs.db
# Файлы, создаваемые при использовании определенных IDE (например, VS Code)
.idea/
.vscode/

# Конфигурационные и секретные файлы (Configuration and secret files)
# ------------------------------------------------------------------
# Файлы с переменными окружения, содержащими секреты (логины, пароли, ключи API).
# Пример файла с безопасными данными (.env.example) можно оставить.
.env
.flaskenv

# Логи и базы данных (Logs and Databases)
# ---------------------------------------
# Файлы логов
*.log
# Локальные файлы базы данных (если не предполагается их версионирование, например, SQLite)
*.sqlite
*.sqlite3

# Тестирование и отчеты (Testing and reports)
# -------------------------------------------
# Отчеты о покрытии кода тестами
.coverage
# Папка с отчетами о тестировании
htmlcov/
.nox/
.tox/

# Сборка и дистрибутивы (Build artifacts)
# ---------------------------------------
# Временные папки сборки
build/
dist/
# Файлы setuptools
*.manifest